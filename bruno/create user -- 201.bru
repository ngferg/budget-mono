meta {
  name: create user -- 201
  type: http
  seq: 2
}

post {
  url: http://localhost:3000/users
  body: json
  auth: inherit
}

body:json {
  {
    "email": "{{email}}"
  }
}

assert {
  res.status: eq 201
}

settings {
  encodeUrl: true
  timeout: 0
}
